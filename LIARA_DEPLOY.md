# راهنمای Deploy روی لیارا 🚀

## پیش‌نیازها
1. حساب کاربری در [لیارا](https://liara.ir)
2. نصب Liara CLI:
```bash
npm install -g @liara/cli
# یا
brew install liara
```

## مراحل Deploy

### 1. Build کردن پروژه
```bash
cd "/Users/mohammadmahdi/Documents/music websale/flutter_application_1"
flutter build web --release
```

### 2. Login به لیارا
```bash
liara login
```
ایمیل و API Token خودت رو وارد کن.

### 3. ایجاد اپلیکیشن در لیارا
دو راه داری:

**راه اول: از طریق CLI**
```bash
liara create --app music-sale --platform docker --region germany
```

**راه دوم: از طریق داشبورد**
- برو به [داشبورد لیارا](https://console.liara.ir/apps)
- روی "ایجاد برنامه" کلیک کن
- نام: `music-sale`
- پلتفرم: Docker
- منطقه: آلمان (یا ایران)

### 4. Deploy کردن
```bash
liara deploy --app music-sale --port 80
```

### 5. دسترسی به سایت
بعد از deploy موفق، سایت در این آدرس در دسترس خواهد بود:
```
https://music-sale.liara.run
```

یا اگه دامنه دلخواه می‌خواهی:
```
https://music-sale.iran.liara.run
```

## تنظیمات اضافی (اختیاری)

### اضافه کردن دامنه شخصی
1. برو به Settings → Domains
2. دامنه خودت رو اضافه کن
3. DNS records رو به لیارا point کن

### مشاهده لاگ‌ها
```bash
liara logs --app music-sale
```

### Restart کردن
```bash
liara restart --app music-sale
```

## Update کردن اپلیکیشن

وقتی تغییراتی دادی:
```bash
# Build جدید
flutter build web --release

# Deploy مجدد
liara deploy --app music-sale --port 80
```

## نکات مهم

✅ **مزایا:**
- سرعت بالا در ایران
- SSL رایگان
- قیمت مناسب
- پشتیبانی فارسی

⚠️ **توجه:**
- فایل‌های upload در container ذخیره نمیشن (نیاز به object storage)
- برای production، نیاز به backend واقعی (Supabase) داری

## هزینه
- پلن رایگان: محدودیت‌های ترافیک
- پلن پایه: ~۵۰ هزار تومان/ماه
- بسته به ترافیک

---
موفق باشی! 🎉
